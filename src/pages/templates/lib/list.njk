{% macro make(list, makeLi, id = false) %}
<ul id="{{ id if id }}">
  {% for item in list %}
  <li>
    {{ makeLi(item) }}

    {% if (list | typeOf) === 'array' %}
      its string
    {% endif %}
  </li>
  {% endfor %}
</ul>
{% endmacro %}
